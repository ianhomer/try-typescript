// my has been defined in my.d.ts. Implementation is not in place, however the
// signature is defined and TypeScript can apply checks and IDE support.
my.log("hello");

// Overload functions - hover over with IDE to see signature.
doOverloaded(1);
doOverloaded("test");

my.getSomething();
